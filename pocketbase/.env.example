# 复制为 .env 或在 systemd / docker 中注入环境变量

# iOS Bundle ID，须与 Xcode 中 PRODUCT_BUNDLE_IDENTIFIER 一致
APPLE_BUNDLE_ID=com.rosen.pengpeng

# 设为 true 时额外请求 Apple JWKS 校验 token header 中的 kid（不替代 RS256 验签）
# APPLE_JWT_CHECK_JWKS=true
