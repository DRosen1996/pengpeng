import SwiftUI

enum AppTheme {
    static let background = Color(white: 0.96)
    static let surface = Color.white
    static let mapBase = Color(white: 0.92)
    static let mapGrid = Color(white: 0.86)
    static let primaryText = Color.black
    static let secondaryText = Color(white: 0.45)
    static let tertiaryText = Color(white: 0.62)
    static let accent = Color.black
    static let cardDark = Color.black
    static let cardDarkSecondary = Color.white.opacity(0.72)
    static let divider = Color(white: 0.9)
    static let zonePulse = Color.black.opacity(0.08)
    static let chipBackground = Color(white: 0.94)
    static let bubbleMine = Color.black
    static let bubbleOther = Color(white: 0.94)

    static let cornerRadius: CGFloat = 16
    static let cardRadius: CGFloat = 20
    static let buttonHeight: CGFloat = 50
}
